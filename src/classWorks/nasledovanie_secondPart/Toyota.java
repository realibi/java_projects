package classWorks.nasledovanie_secondPart;

public class Toyota extends Car {
    public Toyota(String model, int maxSpeed, int price, double fuel_consumption) {
        super(model, maxSpeed, price, fuel_consumption);
    }
}
